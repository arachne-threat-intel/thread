[project]
requires-python = ">=3.8"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
# Ruff 0.16 widened its defaults from 59 rules to 413. State the set we lint
# against so CI doesn't shift under us on the next release.
select = ["E4", "E7", "E9", "F"]
